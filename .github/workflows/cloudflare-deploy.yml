name: Deploy to Cloudflare

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  run-ci-tests:
    # Call the reusable CI workflow
    uses: ./.github/workflows/ci.yml
    # Pass secrets required by the test job in ci.yml
    secrets: inherit

  build-docker:
    # Depend on the reusable CI tests job
    needs: run-ci-tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          load: true
          tags: eigenlayer-ai-agent:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          file: Dockerfile
          platforms: linux/amd64
      
      - name: Export Docker image
        run: |
          mkdir -p ./dist
          docker save eigenlayer-ai-agent:${{ github.sha }} | gzip > ./dist/agent-image.tar.gz
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: agent-image
          path: ./dist/agent-image.tar.gz
          retention-days: 1

  deploy-container:
    # Updated dependency: Only depends on build-docker now
    needs: build-docker
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: agent-image
          path: ./dist
      
      - name: Install Cloudflare Wrangler
        run: npm install -g wrangler
      
      - name: Deploy container to Cloudflare Workers
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          API_KEY: ${{ secrets.API_KEY }}
          AGENT_PRIVATE_KEY: ${{ secrets.AGENT_PRIVATE_KEY }}
        run: |
          # Load the image
          gunzip -c ./dist/agent-image.tar.gz | docker load
          
          # Deploy to Cloudflare using the correct command for containers
          wrangler deploy \
            --name eigenlayer-ai-agent \
            --container \
            --image eigenlayer-ai-agent:${{ github.sha }} \
            --var API_KEY:"${API_KEY}" \
            --var AGENT_PRIVATE_KEY:"${AGENT_PRIVATE_KEY}" 