{
  "name": "eigenlayer-ai-agent-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker with Pyodide to run Python agent code",
  "main": "worker.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  },
  "type": "module"
} 