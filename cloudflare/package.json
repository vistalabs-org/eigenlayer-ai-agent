{
  "name": "eigenlayer-ai-agent-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for EigenLayer AI Agent",
  "main": "worker.js",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start-local": "wrangler dev --local --test-scheduled",
    "trigger-scheduled": "wrangler dev --local --test-scheduled",
    "debug": "wrangler dev --local --test-scheduled --inspector-port=9229"
  },
  "dependencies": {
    "wrangler": "^4.0.0"
  }
} 